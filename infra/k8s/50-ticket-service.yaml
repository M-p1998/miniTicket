apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-service
  namespace: miniticket
spec:
  replicas: 1
  selector:
    matchLabels: { app: ticket-service }
  template:
    metadata:
      labels: { app: ticket-service }
    spec:
      containers:
        - name: ticket-service
          image: miniticketacr12345.azurecr.io/miniticket/ticket-service:latest
          ports: [{ containerPort: 8080 }]
          env:
            - name: SERVER_PORT
              value: "8080"
            - name: DB_URL
              value: jdbc:postgresql://postgres-ticket:5432/ticket_service
            - name: DB_USER
              valueFrom: { secretKeyRef: { name: miniticket-secrets, key: POSTGRES_USER } }
            - name: DB_PASS
              valueFrom: { secretKeyRef: { name: miniticket-secrets, key: POSTGRES_PASSWORD } }
            - name: KC_ISSUER
              # value: http://keycloak:8080/realms/miniTicket
              value: http://4.153.75.53:8080/realms/miniTicket
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
            - name: KAFKA_BOOTSTRAP
              value: kafka:9092
---
apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: miniticket
spec:
  selector: { app: ticket-service }
  ports:
    - port: 8080
      targetPort: 8080
